//
// Created by 倔强的猴子 on 2026/4/13.
//
#include <iostream>

/*int main() {
    // 因为有一个是符号位
    // 整数的数据类型 short的范围 ~2 ^ 15 - 2 ^ 15 - 1
    std::cout << "short:" << sizeof(short) << "字节" << sizeof(short) * 8 << "位" << std::endl;
    // int的范围 -2 ^ 31 - 2 ^ 31 - 1
    std::cout << "int:" << sizeof(int) << "字节" << sizeof(int) * 8 << "位" << std::endl;
    // long的字节数 windows 4字节 32位 范围 -2 ^ 31 - 2 ^ 31 - 1
    //            非windows 32位的操作系统 4字节 32位 -2 ^ 31 - 2 ^ 31 - 1
    //            非windows 64位的操作系统 8字节 64位 -2 ^ 63 - 2 ^ 63 - 1
    std::cout << "long:" << sizeof(long) << "字节" << sizeof(long) * 8 << "位" << std::endl;
    // long long类型：8字节 64位 范围 - 2 ^ 63 - 2 ^ 63 - 1
    std::cout << "long long:" << sizeof(long long) << "字节" << sizeof(long long ) * 8 << "位" << std::endl;

    // 浮点型 就是小数 4个字节 32位 小数点后七位
    std::cout << "float:" << sizeof(float) << "字节" << sizeof(float) * 8 << "位" << std::endl;
    // 8字节 64位 小数点后15位
    std::cout << "double:" << sizeof(double) << "字节" << sizeof(double) * 8 << "位" << std::endl;

    // bool类型 一个字节 8位
    std::cout << "bool:" << sizeof(bool) << "字节" << sizeof(bool) * 8 << "位" << std::endl;

    // 字符类型 一个字节 8位
    std::cout << "char:" << sizeof(char) << "字节" << sizeof(char) * 8 << "位" << std::endl;
    // 字符串类型 用关键字 string来定义
    return 0;
}
*/